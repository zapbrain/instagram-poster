name: Ping Render App

on:
  schedule:
    - cron: '*/3 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Render App
        run: curl -X GET https://instagram-poster-davd.onrender.com
